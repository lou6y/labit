{% extends 'backend/baseback.html.twig' %}

{% block title %}Modifier un utilisateur{% endblock %}

{% block content %}
    <h1>Modifier un utilisateur</h1>
    {{ form(userForm) }}
    <a style="max-width: 300px; margin-left: auto; margin-right: auto;" class="btn btn-block btn-outline-info" href="{{ path('users')}}">back to list</a>
{% endblock %}