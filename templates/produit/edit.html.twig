{% extends 'backend/baseback.html.twig' %}

{% block title %}Edit Produit{% endblock %}

{% block content %}
    <h1>Edit Produit</h1>

    {{ include('produit/_form.html.twig', {'button_label': 'Update'}) }}

    <a style="max-width: 300px; margin-left: auto; margin-right: auto;" class="btn btn-block btn-outline-info" href="{{ path('produit_back') }}">back to list</a>

    {{ include('produit/_delete_form.html.twig') }}
{% endblock %}
